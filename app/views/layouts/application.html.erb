<!DOCTYPE html>
<html lang="en" class="bg-blue-800">
  <head>
    <title>Magz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= yield :current_person %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="flex px-4 py-8 text-gray-900 bg-blue-800">
    <div class="flex flex-col flex-1 min-w-0 min-h-screen px-2 pt-4 pb-10 mx-auto overflow-hidden overflow-x-auto bg-gray-200 border-2 max-w-screen-lg md:px-4 space-y-5 rounded-3xl">
      <div class="relative flex items-center justify-between flex-shrink-0 pb-3 border-b border-gray-400">
        <div class="flex items-center justify-between w-full">
          <%= render "layouts/navigation" %>
        </div>

        <% if flash.any? %>
          <% flash.each do |message_type, message| %>
            <div class="absolute inline-block pt-8 inset-y-8 flash">
              <p class="<%= message_type %> px-4 py-2 font-medium text-center text-white col-span-full rounded-2xl"><%= message %></p>
            </div>
          <% end %>
        <% end %>
      </div>

      <div class="flex-1 py-10 grid grid-cols-12 space-y-4">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
