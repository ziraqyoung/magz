<% recipient = private_conv_recipient(conversation)  %>

<%= render 'shared/conversations/listings',  
  listing_type: conversation,
  listing_avatar_name: recipient.name,
  listing_name: recipient.name %>
